from collectionsapp.models import BeverageType, BottleCap, Collection, CollectionItemImage
from django.http import JsonResponse


def migrate_images(request):
    print("server side function run")

    return None
